/* ===== FUNCIONES ESPECÍFICAS PARA GEOMETRÍA ===== */

// Función para renderizar misiones de geometría (placeholder)
function renderizarMisionGeometria(data) {
    // Esta función se puede expandir cuando se agreguen misiones específicas de geometría
    return `<p class="geometria-placeholder">Misión de geometría en desarrollo...</p>`;
}

// Función para calificar misiones de geometría (placeholder)
function calificarMisionGeometria(misionDiv, misionData) {
    // Esta función se puede expandir cuando se agreguen misiones específicas de geometría
    return 1; // Auto-pass por ahora
}


